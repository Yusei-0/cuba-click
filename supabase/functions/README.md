# Database Functions

This folder contains SQL scripts for database functions, triggers, and stored procedures.

## Structure
- Each function should ideally be in its own file or grouped by logical module.
- Use `migraions/` for table structure changes.
